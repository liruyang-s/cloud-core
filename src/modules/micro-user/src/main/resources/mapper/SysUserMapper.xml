<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.micro.cloud.user.mapper.SysUserMapper">

    <resultMap id="SysUserResult" type="com.micro.cloud.user.domain.SysUser">
        <id property="userId" column="user_id"/>
        <result property="deptId" column="dept_id"/>
        <result property="userName" column="user_name"/>
        <result property="nickName" column="nick_name"/>
        <result property="email" column="email"/>
        <result property="phonenumber" column="phonenumber"/>
        <result property="sex" column="sex"/>
        <result property="avatar" column="avatar"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
        <result property="loginIp" column="login_ip"/>
        <result property="loginDate" column="login_date"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
        <result property="delFlag" column="del_flag"/>
        <result property="deptName" column="dept_name"/>
    </resultMap>

    <!-- 用户列表查询（带数据权限过滤，由 DataScopeInterceptor 改写 SQL） -->
    <select id="selectUserList" resultMap="SysUserResult">
        SELECT u.user_id, u.dept_id, u.user_name, u.nick_name, u.email, u.phonenumber,
               u.sex, u.avatar, u.status, u.login_ip, u.login_date, u.create_by,
               u.create_time, u.remark, d.dept_name
        FROM sys_user u
        LEFT JOIN sys_dept d ON u.dept_id = d.dept_id
        WHERE u.del_flag = '0'
        <if test="userName != null and userName != ''">
            AND u.user_name LIKE CONCAT('%', #{userName}, '%')
        </if>
        <if test="phonenumber != null and phonenumber != ''">
            AND u.phonenumber LIKE CONCAT('%', #{phonenumber}, '%')
        </if>
        <if test="status != null and status != ''">
            AND u.status = #{status}
        </if>
        <if test="deptId != null">
            AND (u.dept_id = #{deptId} OR u.dept_id IN
                (SELECT t.dept_id FROM sys_dept t WHERE find_in_set(#{deptId}, t.ancestors)))
        </if>
        ORDER BY u.user_id
    </select>

    <select id="selectByUserName" resultMap="SysUserResult">
        SELECT u.user_id, u.dept_id, u.user_name, u.nick_name, u.email, u.phonenumber,
               u.sex, u.avatar, u.password, u.status, u.login_ip, u.login_date, u.remark
        FROM sys_user u
        WHERE u.user_name = #{userName} AND u.del_flag = '0'
    </select>
</mapper>
