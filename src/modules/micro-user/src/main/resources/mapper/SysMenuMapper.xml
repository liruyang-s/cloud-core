<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.micro.cloud.user.mapper.SysMenuMapper">

    <select id="selectMenusByUserId" resultType="com.micro.cloud.user.domain.SysMenu">
        SELECT DISTINCT m.menu_id, m.parent_id, m.menu_name, m.path, m.component, m.query,
               m.is_frame, m.is_cache, m.menu_type, m.visible, m.status, m.perms, m.icon, m.order_num
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND m.menu_type IN ('M', 'C')
          AND m.status = '0'
        ORDER BY m.parent_id, m.order_num
    </select>

    <select id="selectPermsByUserId" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND m.status = '0'
          AND m.perms IS NOT NULL
          AND m.perms != ''
    </select>

    <select id="selectMenuIdsByRoleId" resultType="java.lang.Long">
        SELECT m.menu_id
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        WHERE rm.role_id = #{roleId}
    </select>
</mapper>
